# EnvHaven Environment Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# SECURITY: Never commit .env to version control!

# ===================
# Core Settings
# ===================
PUID=1000
PGID=1000
TZ=America/New_York
DEFAULT_WORKSPACE=/config/workspace

# Set to true to disable web UI (SSH-only mode)
# ENVHAVEN_DISABLE_WEBUI=true

# ===================
# Authentication (REQUIRED)
# ===================
# Web GUI password - set a strong password
PASSWORD=

# Sudo/SSH password - set a strong password
SUDO_PASSWORD=

# Or use hashed passwords for better security:
# HASHED_PASSWORD=       # Generate: echo -n "pass" | npx argon2-cli -e
# SUDO_PASSWORD_HASH=    # Generate: openssl passwd -6

# ===================
# SSH Configuration
# ===================
# RECOMMENDED: Import your SSH keys directly from GitHub!
# Just enter your GitHub username - all your public keys are automatically loaded.
PUBLIC_KEY_URL=https://github.com/yourusername.keys

# Alternative: Paste a specific public key directly
# PUBLIC_KEY=ssh-ed25519 AAAA... user@host

# Alternative: Mount a directory of key files
# PUBLIC_KEY_DIR=/config/.ssh/authorized_keys.d

# ===================
# AI Tool API Keys
# ===================
# Set the keys for the AI tools you want to use.
# Leave empty if not using that tool.

ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# GOOGLE_API_KEY=
# MISTRAL_API_KEY=
